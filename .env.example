# Chave da IA usada pela analise automatica (route handler /api/analise).
# Mesma chave do Ollama Cloud usada no SyncAgenda.
# Copie este arquivo para .env.local e preencha:
#   cp .env.example .env.local
OLLAMA_API_KEY=
